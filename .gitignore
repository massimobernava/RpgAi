# Build artifacts
build/

# macOS metadata
.DS_Store
**/.DS_Store

# Claude Code project data
.claude/

# Save files (game sessions)
saves/
*.jsonl
*.jsonl copia

# Claude Code project instructions (internal, not for public repo)
CLAUDE.md

# Generated scene images (cache rebuilt at runtime)
scripts/images/

# Launch scripts containing API keys.
# Commit a *.sh.template with placeholders instead.
openrouter_web.sh
console_test.sh
final_test_narration.sh
rpgai_qwen36_remote.sh

# Settings file — contains API keys
rpgai_settings.json

# GitHub token
.github_token

# Private game scripts (not published — only fantasy_demo.lua is public)
npc_mod/
scripts/lib/npcs/
scripts/estate_italiana_*.lua
scripts/snow_daze_*.lua
scripts/villa*.lua
scripts/sogno_lucido_*.lua
scripts/lib/estate_data.lua
scripts/lib/estate_dataX.lua
scripts/lib/estate*
scripts/data/
scripts/plaza_data/
scripts/summertime_data/
narrative/
guida/

# Internal playtesting log (contains private script details)
PLAYTESTING_INTERNAL.md
PLAYTESTING_OPENCODE_DEEPSEEK.md
test_api.py

# Old pipeline (kept locally as reference, superseded by pipeline/)
pipeline_old/

# Pipeline working directory — contains generated/private story data
pipeline_stages/

# Native pipeline (WIP — not ready for public release)
pipeline_native/
installer/macos/RpgAi.app/
installer/macos/app-template/

# Ancora in fase di test
tts_locale/
